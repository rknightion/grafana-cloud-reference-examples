# yaml-language-server: $schema=https://coderabbit.ai/integrations/schema.v2.json
# Repository override. Central org defaults are inherited; entries here supplement them.
inheritance: true
reviews:
  tools:
    # Not configured in this repository. The org config no longer mutes it fleet-wide,
    # because muting a linter centrally strips it from repos that do ship its config.
    oxc:
      enabled: false
    # Not configured in this repository. The org config no longer mutes it fleet-wide,
    # because muting a linter centrally strips it from repos that do ship its config.
    biome:
      enabled: false
