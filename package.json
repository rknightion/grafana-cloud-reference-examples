{
  "name": "grafana-cloud-reference-examples",
  "version": "0.0.0",
  "private": true,
  "description": "Reference examples for integrating systems with Grafana Cloud",
  "license": "Apache-2.0",
  "type": "module",
  "workspaces": [
    "common/nodejs"
  ],
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "fmt": "prettier --write .",
    "fmt-check": "prettier --check .",
    "lint": "eslint . && npm run --workspaces --if-present typecheck",
    "test": "npm run --workspaces --if-present test"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.0",
    "@types/node": "^24.9.0",
    "eslint": "^9.39.0",
    "prettier": "^3.6.2",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.46.0",
    "vitest": "^3.2.4"
  }
}
