name: scorecard

on:
  push:
    branches: [main]
  schedule:
    - cron: "27 4 * * 1"
  workflow_dispatch: {}

permissions: {}

jobs:
  analysis:
    permissions:
      security-events: write # upload the SARIF results
      id-token: write # OIDC, to publish results and the badge to the OpenSSF API
    uses: rknightion/.github/.github/workflows/scorecard.yml@4d749e93fb2ba8ae3d213c21686bc7ac204bb928 # v1.23.0
