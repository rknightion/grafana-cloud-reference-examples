name: scorecard

on:
  push:
    branches: [main]
  schedule:
    - cron: "27 4 * * 1"
  workflow_dispatch: {}

permissions: {}

jobs:
  analysis:
    permissions:
      security-events: write # upload the SARIF results
      id-token: write # OIDC, to publish results and the badge to the OpenSSF API
    uses: rknightion/.github/.github/workflows/scorecard.yml@e6d074e42fed0c74b1e9936637b7e2fb21403779 # v1.22.0
