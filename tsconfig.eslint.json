{
  "//": "Lint-only project. The build config (common/nodejs/tsconfig.json) covers src only, so tests are never emitted into dist; this one covers everything eslint needs to type-check.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "composite": false,
    "declaration": false,
    "declarationMap": false,
    "types": ["node"]
  },
  "files": [],
  "include": ["common/nodejs/src/**/*.ts", "common/nodejs/test/**/*.ts"],
  "exclude": ["**/dist/**", "**/node_modules/**"]
}
